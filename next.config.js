/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    // Keep ignoring TS build errors if you want faster builds
    ignoreBuildErrors: true,
  },
}

module.exports = nextConfig
